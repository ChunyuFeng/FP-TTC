# ===== FP-TTC pip-only requirements (install with --no-deps) =====
# 说明：numpy/scipy/matplotlib/torch/mmcv/opencv 由 conda 安装并锁版本，避免 pip 改动 ABI/CUDA。

addict==2.4.0
einops==0.6.1
imageio==2.31.5
importlib-resources==6.1.0
packaging==23.1
pypng==0.20220715.0
pyyaml==6.0
tomli==2.0.1
tqdm==4.66.2
yapf==0.33.0
zipp==3.17.0

neptune==1.14.0
pyquaternion==0.9.9
scikit-learn==1.3.2
nuscenes-devkit==1.1.11

albumentations==2.0.8

# 解决 wheel 0.46.* 需要 packaging>=24 的冲突提示（你锁 packaging==23.1）
wheel<0.46